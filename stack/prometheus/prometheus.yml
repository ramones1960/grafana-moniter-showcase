global:
  scrape_interval: 5s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  # シミュレータが公開する衛星ヘルスの業務メトリクス + プロセスメトリクス
  # （sat ラベルはアプリ側メトリクスが持つためここでは付与しない）
  - job_name: satellite
    static_configs:
      - targets: ["simulator:8000"]
        labels:
          mission: spaceops

  # Prometheus 自身
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
